# Web framework
Flask>=2.2,<3

# Database / ORM for SQLite
Flask-SQLAlchemy>=3.0,<4

# Local development env vars
python-dotenv>=0.21

# Image handling for label composition
Pillow>=9.0

# Barcode generation (no QR codes)
python-barcode>=0.14

# PDF generation for printable labels
reportlab>=4.0

# WSGI server for production / Docker
gunicorn>=20.1

# Windows EXE packaging
pyinstaller>=5.0
pystray>=0.19  # System tray icon support
Pillow>=9.0  # Required for pystray (already listed above)

# Testing (dev)
pytest>=7.0

# Live reload for development
flask-livereload>=0.2
